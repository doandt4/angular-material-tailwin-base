import { loggedInUser } from "./loggedInUser";

export interface File3D {
    createAt: string;
    id: string;
    key: string;
    updateAt: string;
    url: string;
    userId: string;
    filename: string;
}